services:

  live-streame-kafka:
    image: apache/kafka:latest
    container_name: data-bro
    ports:
      - "9092:9092"

    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller

      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://live-streame-kafka:9092

      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@live-streame-kafka:9093

      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_NUM_PARTITIONS: 1

    healthcheck:
      test:
        [
          "CMD-SHELL",
          "/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server localhost:9092"
        ]
      interval: 5s
      timeout: 5s
      retries: 10

    networks:
      - my-net


  es:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.15.0

    environment:
      discovery.type: single-node
      xpack.security.enabled: "false"
      ES_JAVA_OPTS: "-Xms1g -Xmx1g"

    ports:
      - "9200:9200"

    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -s http://localhost:9200/_cluster/health | grep -q '\"status\"'"
        ]
      interval: 10s
      retries: 12

    networks:
      - my-net


  kib:
    image: docker.elastic.co/kibana/kibana:8.15.0

    environment:
      ELASTICSEARCH_HOSTS: http://es:9200

    ports:
      - "5601:5601"

    depends_on:
      es:
        condition: service_healthy

    networks:
      - my-net


  python-producer:
    build:
      context: ./Py_Data_producer

    environment:
      KAFKA_BOOTSTRAP_SERVERS: live-streame-kafka:9092

    depends_on:
      live-streame-kafka:
        condition: service_healthy

    networks:
      - my-net


  consumer:
    build:
      context: ./csConsumer

    environment:
      Kafka__BootstrapServers: live-streame-kafka:9092
      Kafka__GruopId: testGruop1
      Kafka__Topics__Raw-data: raw-data

      ElasticSearch__Endpoint: http://es:9200

    depends_on:
      live-streame-kafka:
        condition: service_healthy
      es:
        condition: service_healthy

    networks:
      - my-net
  
  report-api:
    build:
      context: ./ReportApi
    environment:
      ElasticSearch__Endpoint: http://es:9200
    ports:
      - "8000:8080"
    depends_on:
      es:
        condition: service_healthy
    networks:
      - my-net


networks:
  my-net:
    driver: bridge